services:
  redis:
    container_name: shared_redis
    image: redis:latest
    restart: always
    network_mode: host
    command: redis-server --save 60 1 --loglevel warning
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
